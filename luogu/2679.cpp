#include <iostream>
#include <string>
#include <vector>

using LLI = long long int;
int main() {
  const int mod_value = 1000000007;
  int n, m, k;
  std::string str_a, str_b;
  std::cin >> n >> m >> k;
  std::cin >> str_a;
  std::cin >> str_b;
  std::vector<std::vector<LLI>> dp_array;
  std::vector<std::vector<LLI>> sum;

  return 0;
}